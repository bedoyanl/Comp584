// Importing necessary modules for the code
import { createApp } from "vue"; // importing the createApp function from the vue module
import QuizGenerator from "./QuizGenerator.vue"; // importing the QuizGenerator component/application

// Creates a new instance of QuizGenerator and mounts the application to the div with the id "app"
createApp(QuizGenerator).mount("#app");
